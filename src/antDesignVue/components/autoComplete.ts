export const key = ['autoComplete ', 'complete', 'auto', '自动完成', '自动'];
export const value =
  '<a-auto-complete v-model:value="modalValue" placeholder="" />';

export const autoComplete = {
  key,
  value,
};
